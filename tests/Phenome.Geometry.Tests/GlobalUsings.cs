// The kernel's namespaces follow its folders, so a project written against it imports both halves: the geometry
// types and the modules of functions over them. They are always used together, so this is a fact about the project
// rather than about any one file in it.

global using Phenome.Geometry;
global using Phenome.Geometry.Modules;
global using Phenome.Geometry.Types;
